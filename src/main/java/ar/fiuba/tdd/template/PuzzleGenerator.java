package ar.fiuba.tdd.template;

public class PuzzleGenerator {

    public void startGeneration() {

/*      Parser parser = new Parser();
        parser.decodeJson();
        int width = parser.getWidth();
        int height = parser.getHeight();
        ArrayList<Cell> clues = parser.getClues();
        ArrayList<Cell> solution = parser.getSolution();
        ArrayList<String> rules = parser.getRules();

*/
        // must generate rules using the ArrayList of rules returned by the parser
        // Puzzle puzzle = new Puzzle(parser.getWidth(), parser.getHeight(), firstRule); //must merge with develop


    }
}
